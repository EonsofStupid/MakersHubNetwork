
import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card';

export const BuildManagement = () => {
  return (
    <Card className="cyber-card">
      <CardHeader>
        <CardTitle>Build Management</CardTitle>
      </CardHeader>
      <CardContent>
        Build management interface will be implemented here
      </CardContent>
    </Card>
  );
};
