
import { safelyRenderLogNode as safelyRenderNode, nodeToSearchableString } from '@/logging';

/**
 * Re-export the core utilities from the logging system
 * to maintain backward compatibility
 */
export { safelyRenderNode, nodeToSearchableString };
