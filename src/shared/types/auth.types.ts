
// This file is a re-export to avoid duplication
// We're re-exporting Auth types from shared.types.ts

import { AuthStatus, AuthEvent, AuthEventType, User, UserProfile, UserRole } from './shared.types';

// Re-export all auth types
export { AuthStatus, AuthEvent, AuthEventType, User, UserProfile, UserRole };
