
import { usePerformanceLogger } from '@/logging/hooks/usePerformance';

/**
 * Re-export the performance logger hook from the centralized logging system
 * to maintain backward compatibility
 */
export { usePerformanceLogger };
