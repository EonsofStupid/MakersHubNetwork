
// Re-export auth bridge from bridges directory
import { authBridge, subscribeToAuthEvents, publishAuthEvent } from '@/bridges/AuthBridge';

export { authBridge, subscribeToAuthEvents, publishAuthEvent };
